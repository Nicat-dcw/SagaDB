{
  "name": "sagadb",
  "version": "1.0.0",
  "module": "dist/db.js",
  "main": "dist/db.js",
  "type": "module",
  "types": "./src/types/types.ts",
  "exports": {
    ".": "./dist/db.js"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir ./dist",
    "test": "bun test",
    "benchmark": "bun run src/benchmark.ts"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "node-cache": "^5.1.2"
  },
  "homepage": "https://github.com/Nicat-dcw/sagadb",
  "keywords": ["local database", "local storage", "local db", "local storage", "local db", "local storage", "local db", "local storage", "local db","Json Database","Local Database","Local Storage","Local DB","Local Storage","Local DB","Local Storage","Local DB","Local Storage","Local DB","Json Database", "Database"],
  "devDependencies": {
    "@types/node": "^20.17.9",
    "bun-types": "latest",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.7.2"
  }
}
